apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: inference
        image: inference-service:latest
        resources:
          limits:
            nvidia.com/gpu: 1
